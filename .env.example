# ==========================================
# Variáveis de Ambiente - Projeto Completo
# ==========================================
# IMPORTANTE: Copie este arquivo para .env e preencha com seus valores
# Este arquivo é apenas um exemplo e será ignorado pelo Git

# ==========================================
# Banco de Dados
# ==========================================
# Para Docker Compose, use 'db' como host (nome do serviço no docker-compose.yml)
# Para desenvolvimento local, use 'localhost'
DATABASE_URL="postgresql://postgres:postgres@db:5432/tasks_db?schema=public"

# ==========================================
# JWT - Autenticação
# ==========================================
# Gere uma chave secreta forte e segura!
# Você pode gerar uma com: openssl rand -base64 32
# OU: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET="sua_chave_secreta_super_segura_aqui_altere_esta_chave"

# ==========================================
# Servidor
# ==========================================
# Porta do servidor Express (opcional, padrão é 3000)
PORT=3000

# ==========================================
# Ambiente
# ==========================================
# Valores: development, production, test
NODE_ENV="development"
